# Alpha Note

Strata tracks source documents, computes fingerprints, and rebuilds only the affected
parts of a pipeline when source content or configuration changes.

This note is long enough to produce at least one chunk for the example pipeline.
